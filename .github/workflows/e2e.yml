name: E2E smoke test

on:
  pull_request:

permissions:
  contents: read

concurrency:
  group: e2e-pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true

# Every job runs the live flow against the real service, so each one needs the identification
# key. GitHub withholds secrets from pull requests opened from a fork, so those are skipped.
# The key lives in the `e2e` environment, reachable only from these jobs.
jobs:
  web:
    name: Web
    if: github.event.pull_request.head.repo.full_name == github.repository
    environment: e2e
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      API_KEY: ${{ secrets.API_KEY }}
      REGION: ${{ secrets.REGION }}
      ENDPOINT: ${{ secrets.ENDPOINT }}
      SCRIPT_URL_PATTERN: ${{ secrets.SCRIPT_URL_PATTERN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Install and set Flutter version
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2 #v2.23.0
        with:
          flutter-version: 3.44.0
          channel: stable
      - name: Restore packages
        working-directory: example
        run: flutter pub get
      - name: Write .env.local
        run: ./.github/scripts/write_e2e_env.sh
      - name: Run smoke test
        working-directory: example
        run: |
          CHROMEDRIVER="$CHROMEWEBDRIVER/chromedriver"
          test -x "$CHROMEDRIVER"
          "$CHROMEDRIVER" --port=4444 > "$RUNNER_TEMP/chromedriver.log" 2>&1 &
          CHROMEDRIVER_PID=$!
          trap 'kill "$CHROMEDRIVER_PID" 2>/dev/null || true' EXIT
          ../.github/scripts/retry.sh flutter drive \
            --driver=test_driver/integration_test.dart \
            --target=integration_test/smoke_test.dart \
            --web-port=3000 \
            -d web-server

  android:
    name: Android
    if: github.event.pull_request.head.repo.full_name == github.repository
    environment: e2e
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      API_KEY: ${{ secrets.API_KEY }}
      REGION: ${{ secrets.REGION }}
      ENDPOINT: ${{ secrets.ENDPOINT }}
      SCRIPT_URL_PATTERN: ${{ secrets.SCRIPT_URL_PATTERN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Install and set Flutter version
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2 #v2.23.0
        with:
          flutter-version: 3.44.0
          channel: stable
      - name: Restore packages
        working-directory: example
        run: flutter pub get
      - name: Write .env.local
        run: DISABLE_LOCATION_COLLECTION=true ./.github/scripts/write_e2e_env.sh
      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' \
            | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
      - name: Run smoke test
        uses: ReactiveCircus/android-emulator-runner@1dcd0090116d15e7c562f8db72807de5e036a4ed
        with:
          api-level: 35
          arch: x86_64
          profile: pixel_6
          working-directory: example
          script: ../.github/scripts/retry.sh flutter test integration_test/smoke_test.dart -d emulator-5554

  ios:
    name: iOS
    if: github.event.pull_request.head.repo.full_name == github.repository
    environment: e2e
    runs-on: macos-15
    timeout-minutes: 30
    env:
      API_KEY: ${{ secrets.API_KEY }}
      REGION: ${{ secrets.REGION }}
      ENDPOINT: ${{ secrets.ENDPOINT }}
      SCRIPT_URL_PATTERN: ${{ secrets.SCRIPT_URL_PATTERN }}
    # Checkout as `fpjs_pro_plugin`: Flutter SwiftPM identifies path-overridden plugins by folder name
    # (https://github.com/flutter/flutter/issues/186881). Without this the build falls back to CocoaPods.
    # TODO: drop `path:` and this `defaults:` block once flutter#188647 reaches stable.
    defaults:
      run:
        working-directory: fpjs_pro_plugin
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          path: fpjs_pro_plugin
      # iOS runs on latest stable only, unlike the other jobs which pin the 3.44.0 floor.
      # Pinned to 3.44.0 the example compiles but never starts on the simulator, so `flutter test`
      # waits for a connection until the job times out. `ci.yml` covers the floor.
      - name: Install Flutter latest stable
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2 #v2.23.0
        with:
          channel: stable
      - name: Restore packages
        working-directory: fpjs_pro_plugin/example
        run: flutter pub get
      - name: Write .env.local
        run: DISABLE_LOCATION_COLLECTION=true ./.github/scripts/write_e2e_env.sh
      - name: Start iOS simulator
        id: simulator
        uses: futureware-tech/simulator-action@e89aa8f93d3aec35083ff49d2854d07f7186f7f5
        with:
          model: iPhone 16
          wait_for_boot: true
      - name: Run smoke test
        working-directory: fpjs_pro_plugin/example
        run: |
          ../.github/scripts/retry.sh flutter test integration_test/smoke_test.dart \
            -d "${{ steps.simulator.outputs.udid }}"
      # `flutter test` succeeds even if SwiftPM never picked this plugin up. Assert that it did.
      - name: Verify Swift Package Manager was used
        run: |
          manifest=example/ios/Flutter/ephemeral/Packages/FlutterGeneratedPluginSwiftPackage/Package.swift
          if ! grep -q 'fpjs_pro_plugin' "$manifest"; then
            echo "$manifest does not depend on fpjs_pro_plugin, SwiftPM did not pick the plugin up."
            cat "$manifest"
            exit 1
          fi
