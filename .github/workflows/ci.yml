name: ci
# PRs only (same-repo and forks). A `push` trigger also runs on branch updates and
# duplicates every same-repo PR.
on:
  pull_request:
jobs:
  test:
    name: Test
    uses: ./.github/workflows/build-and-run-check-workflow.yml
