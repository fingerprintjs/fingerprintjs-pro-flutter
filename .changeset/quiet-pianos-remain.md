---
"fingerprintjs-pro-flutter": major
---

Replace the static plugin API with an instance `Fingerprint` client, `FingerprintResult`, and `FingerprintError`. Identify on the web with `@fingerprint/agent` v4.
